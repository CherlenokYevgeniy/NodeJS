first task: create server in node.js. create local module. and use this local model in server 

second task: Given a string containing the letters and brackets '(', ')', '[', ']', '{', '}'.
If the brackets are placed correctly (that is, each opening parenthesis corresponds to a closing one of the same type),
then print 'yes', otherwise print the number of the first erroneous bracket or -1 if there is no closing bracket.